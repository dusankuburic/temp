# Environment Variables Template for Temp Application
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# =============================================================================
# JWT Configuration
# =============================================================================
# Generate a strong secret key for JWT token signing
# Use a cryptographically secure random string (minimum 64 characters)
# Example: openssl rand -base64 64
JWT_SECRET=mLBI7dJZ53cYhYvRVHZbLh415i+y5QhLHpl/u1vurr+ewP+nv+EIjEpMplov0NRUyC2nQVk+2PpB5pG4e1aqGQ==

# JWT Token Issuer (should match your API URL)
JWT_ISSUER=https://localhost:5001

# =============================================================================
# Database Configuration
# =============================================================================
# SQL Server connection string for Docker environment
SQL_CONNECTION_DOCKER=Server=tempdb;User=sa;Password=YourStrong!Passw0rd;Database=Temp;Trusted_Connection=false;MultipleActiveResultSets=True;TrustServerCertificate=True;

# SQL Server connection string for local development (without Docker)
SQL_CONNECTION_LOCAL=Server=.;Database=Temp;Trusted_Connection=True;MultipleActiveResultSets=True;TrustServerCertificate=True;

# SQL Server SA Password (for Docker)
SQL_SA_PASSWORD=YourStrong!Passw0rd

# =============================================================================
# Redis Configuration
# =============================================================================
# Redis connection string for Docker environment
REDIS_CONNECTION=redis-cache:6379

# Redis connection string for local development
REDIS_CONNECTION_LOCAL=localhost:6379

# =============================================================================
# Azure Storage Configuration (Azurite for local development)
# =============================================================================
# Azurite connection string for local development (default Azurite credentials)
AZURE_STORAGE_CONNECTION=AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;DefaultEndpointsProtocol=http;BlobEndpoint=http://azurite:10000/devstoreaccount1;QueueEndpoint=http://azurite:10001/devstoreaccount1;TableEndpoint=http://azurite:10002/devstoreaccount1;

# For production, use a real Azure Storage connection string:
# AZURE_STORAGE_CONNECTION=DefaultEndpointsProtocol=https;AccountName=youraccount;AccountKey=yourkey;EndpointSuffix=core.windows.net

# =============================================================================
# Redis Commander UI (for development)
# =============================================================================
REDIS_UI_USER=root
REDIS_UI_PASSWORD=toor

# =============================================================================
# Seeding Configuration
# =============================================================================
SEED_ADMIN_PASSWORD=YourSecurePassword123!

# =============================================================================
# Production Configuration
# =============================================================================
# Redis Password (for production)
REDIS_PASSWORD=YourStrongRedisPassword123!

# Certificate Password (for production API HTTPS)
CERT_PASSWORD=ZH4no7TVVeuzxq6+uLI4ym7yZnA=

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
# 1. NEVER commit the .env file to version control
# 2. Generate new secrets for production environments
# 3. Use strong, unique passwords for all services
# 4. Rotate secrets regularly
# 5. Use Azure Key Vault or similar for production secrets
# 6. Keep this template file updated when adding new environment variables
# 7. For production deployment, use: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
